import css from './project-card.module.scss';

export const ProjectCard = () => {
	const a = '';
	return (
		<div className={css.wrapper}>
			<div className={css.photo} />
			<div className={css.name}>Wakadoo</div>
		</div>
	);
};
